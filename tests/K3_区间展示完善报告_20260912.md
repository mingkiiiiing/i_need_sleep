# K3 区间与校准展示完善报告（2026-09-12）

依据：《不确定性专项审查_20260912/审查结论与解决方案.md》§3；《不确定性校准专项任务看板_20260912.md》K3 段与红线；《区间校准专项台账_20260912.md》B 组 L-ui-01~06。
范围：`src/` 区间/校准文案组件、`backend/app/` 区间元数据与文案层、`tests/`（本报告）。
未改动：模型文件、训练代码、快照缓存语义、gate_table、阈值、台账、看板。

## 0. 红线自查（先声明）

- 未取消任何限制：`decision_usable=false`、`band_range_blocked`、单类别、退化区间拦截逻辑全部保持；`_build_uncertainty` 的 ①结构 → ②校准 → ③决策 判定顺序与判据一字未动。
- 改文案不是校准修复：新增文案只解释"为什么不可用"，未出现"已达标/已校准/修复"字样；未放宽 `coverage_acceptance_min`（仍 88%）。
- 全湖 `heroRange` 非站点模式返回 null 的行为保持（`ForecastResultPanel.vue` heroRange 首行 `if (props.scope !== 'station') return null` 未动）。
- 行级退化只加标记与展示，不参与 `decision_usable`；不写产物文件，只读 `runs/<run>/test_predictions.csv`。

## 1. 改动文件清单

### 前端（src/）
| 文件 | 改动 |
| --- | --- |
| `src/components/heatmap/ForecastResultPanel.vue` | L-ui-01/02/03/04/05/展示层级；新增 `calibrEvidenceText`、`pctText`、`calibShortTitle`、`rowDegeneracyNote`、`relatedBandBlocked`、`blockedCalibEvidence`；`CALIBRATION_TEXT` 补 `single_class_test`；`coverageRing` 欠覆盖写具体数字；区间矩阵行级退化与等级阻断文案；新增 `[data-role]`：`risk-source-separation`、`related-band-blocked`、`row-degeneracy-note`、`band-range-blocked-source` |
| `src/stores/predictionSnapshot.js` | `intervalState` 只读透出 `rowLevelDegeneracy`（不改既有字段与判定） |
| `src/components/heatmap/EvaluationPanel.vue` | `CALIBRATION_TEXT` 补 `single_class_test` 中文映射（L-ui-03） |

### 后端（backend/app/）
| 文件 | 改动 |
| --- | --- |
| `backend/app/algorithm_models.py` | L-ui-05：新增 `_row_level_degeneracy` / `_compute_row_level_degeneracy` 与缓存；`_build_uncertainty` 在 `calibration_evidence` 段输出 `uncertainty.row_level_degeneracy`；`band_range_blocked.note` 由"不给出等级范围"改为"风险等级范围暂不可用"（L-ui-02） |

### 测试/报告
- 新增 `tests/K3_区间展示完善报告_20260912.md`（本文件）。
- 未改任何断言（`tests/browser-smoke.mjs`、`tests/*.test.mjs` 一字未动）。

## 2. 六项展示改动前后对照

证据为渲染后 DOM 文本（站点 mee-0145cdb7，快照 `PRED_mee_surface_water_realtime_20260912T152435Z_a444f274fdf7`）。

### L-ui-01 欠覆盖文案具体化（undercovered）
- 前：`uncertainty-status-detail` = "冻结测试集经验覆盖率低于验收线，区间实际覆盖不足，不构成校准证据。（test_n=132）"，短标题统一为"校准证据不足"。
- 后（站点 chla T+1）：
  - 短标题：`覆盖率未达标`
  - 明细：`区间覆盖率未达标：80.30%，要求≥88%（验收线 88% = 标称 90% − 容差 2%）；区间结构自洽但未通过可信性验收，不构成校准证据。（test_n=132）`
  - 覆盖率环注记：`80.3% 经验覆盖率 区间覆盖率未达标：80.3%，要求≥88%`
- 四类分别说明（不再都停留"证据不足"，`CALIB_SHORT_TITLE` + `calibrEvidenceText`）：
  - 样本不足 → "样本不足：n=X（最小样本 15）…"
  - 单类别 → "单类别测试段：独立测试段只含单一类别样本…"（见 L-ui-03）
  - 无区间 → "无区间 / 该任务未提供 conformal 预测区间"
  - 结构退化 → 走结构层"区间无效"，detail 用 `structural_reason`

### L-ui-02 "不给出范围"→"风险等级范围暂不可用"
- 前：等级范围头部大字 `不给出范围`；原因仅一句"源区间校准证据不足：覆盖率未达标"。
- 后（站点 risk T+1）：
  - 头部：`风险等级范围暂不可用`
  - 原因行：`原因：源区间（叶绿素 a）区间覆盖率未达标：80.30%，要求≥88% ｜留出段经验覆盖率 80.30%（要求≥88%），测试样本 n=132`
  - 新增源证据行 `band-range-blocked-source`：`源区间（叶绿素 a 预测区间，结构自洽）：22.017 — 31.999 μg/L｜测试样本 n=132｜经验覆盖率 80.30%（要求≥88%）`（保留 source_interval_undercovered 与源叶绿素证据链）
  - 后端 `band_range_blocked.note` 同步改为"…因此风险等级范围暂不可用…源区间本身的数值与覆盖率证据照常保留。"
  - 区间矩阵"风险等级"格：由 `无` → `等级范围暂不可用`，title 注明"非『无此任务』"

### L-ui-03 single_class_test 中文映射
- 前：`CALIBRATION_TEXT` 无该键 → `calibrationText` 回退原始字符串 `single_class_test`。
- 后（站点 risk T+30）：短标题/矩阵格 = `单类别测试段`；明细 = `独立测试段只含单一类别样本，覆盖率仅反映该类覆盖，不构成事件判别力证据。（test_n=31）`；覆盖率环注记 = `100% 经验覆盖率 单类别测试段，仅登记`。`EvaluationPanel.vue` 同步补映射。

### L-ui-04 风险概率与风险等级分别标注来源
- 前：仅等级范围头注明"由叶绿素 a 预测区间映射"，未显式说明与风险概率的来源区别。
- 后（risk 页新增 `risk-source-separation`）：`风险概率（上方 P05—P95）来自 T6 概率模型的独立 conformal 区间；风险等级由叶绿素 a（T5）预测区间按冻结风险带映射。两者来源不同、证据链不同，任何一方的可用性都不能替代另一方。`（T+1 与 T+30 均渲染，覆盖"概率 validated 而等级 blocked"的分裂场景）

### L-ui-05 行级 0 宽区间逐行标退化（T4-sug-02）
- 后端：`uncertainty.row_level_degeneracy` 新增，对 frozen bloom/probability（T1/T6，off0 h1/3/7/15）逐行复算：
  - `bloom`/`probability` h1/h3/h7/h15 → `degenerate_rows=25, rows_n=40, degenerate_rate=0.625`（与审计 `t4_interval_check_20260912.json` 的 25/40 一致）
  - chla 等无贴地行 → `degenerate_rows=0`（前端不渲染空提示）
  - 季节气候态 h30+ 无 CSV/残差池 → `null`（不虚构数字）
- 前端：新增 `row-degeneracy-note` = `区间退化（宽度不足）：留出段 25/40 行（62.5%）在物理边界裁剪后宽度为 0，这些行不表达可用不确定性范围；整体校准状态不因此改变，仅提示行级信息量。`
- 区间矩阵：`水华`/`风险概率` 格 = `参考·退化25/40行`，title 追加 `｜行级退化 25/40 行宽度为 0`；整体 `calibration_status=validated` 与 `decision_usable` 均不变。

### 展示层级：参考范围突出 + 等级阻断作相关说明
- 非 risk 指标页新增 `related-band-blocked`：`相关说明：风险等级范围因源区间（叶绿素 a）未达决策可用而暂不可用；本页展示的是 叶绿素 a 自身的区间，结构自洽但未通过可信性验收时按「参考范围」呈现，与该等级阻断相互独立。`
- 站点 chla/density/biomass 结构自洽但未达标时 hero 仍显示"参考范围"（`heroRange` 的 watch 分支未动），等级阻断不再被误读为"当前数值区间不存在"。

## 3. 两项回归测试数字

| 项 | 命令 | 结果 |
| --- | --- | --- |
| 单元/竞态 | `npm.cmd test` | **12 / 12 pass，0 fail**（duration ≈ 0.45s） |
| 浏览器冒烟 | `npm.cmd run test:browser` | **70 / 70 通过**（含 T8 三层合同、T8b 等级阻断数据驱动断言，逐条 PASS） |
| 后端合同（附加） | `pytest backend/tests/test_v03_trust_contract.py` | 22 / 22 passed（三层合同与零宽退化用例未受影响） |

基线（改动前）同样为 12/12 与 70/70，改动后数字不变。冒烟环境：后端 8000（重启后 rebuild 新快照）+ 前端 5173，均在线。

## 4. 未改动的保护项确认（限制未取消清单）

- `decision_usable = structural_valid ∧ (calibration_status == validated)`：判据未动（`algorithm_models.py` `_build_uncertainty`）。
- `band_range_blocked.reason = source_interval_undercovered`：仅改 `note` 文案，reason 值、触发条件、`source_interval` 数值未动。
- `single_class_test` / `insufficient_test_evidence` / `no_test_evidence` 判定：未动；仅补中文映射。
- 覆盖率验收线 `COVERAGE_TARGET=0.90` / `COVERAGE_TOLERANCE=0.02` / `COVERAGE_ACCEPTANCE_MIN=0.88` / `MIN_CALIBRATION_TEST_N=15`：未动。
- 零宽退化 `structural_valid=false`：未动；行级退化是并列的新字段，不回写 `structural_valid`。
- 全湖 `heroRange` 非站点模式 return null：未动（L-ui-06 保护项，防回归）。
- gate_table / manifest / 模型包 / 训练代码 / 快照缓存语义（`CACHE_SCHEMA_VERSION` 未递增，因为展示字段已在重建后的快照内；重建走官方 `/prediction-snapshot/rebuild` 管理员入口）：未改文件。
- 未出现"已达标/已校准/修复"字样。

## 5. 建议入台账条目（供主控）

| 编号 | 明细 | 建议状态 |
| --- | --- | --- |
| L-ui-01 | 欠覆盖文案具体化（覆盖率+88% 验收线；四类分别说明） | 修复待复测 |
| L-ui-02 | "不给出范围"→"风险等级范围暂不可用"+原因+源叶绿素证据链 | 修复待复测 |
| L-ui-03 | single_class_test 中文映射（两处组件） | 修复待复测 |
| L-ui-04 | 风险概率 / 风险等级来源分别标注 | 修复待复测 |
| L-ui-05 | frozen bloom/probability 行级 0 宽退化逐行标记（serving 元数据+前端） | 修复待复测 |
| L-ui-06 | 全湖 heroRange 非站点返回 null | PASS（回归确认，未改） |

## 6. 遗留

1. 行级退化只覆盖**有 `test_predictions.csv` 的 run**（T1/T6/T3/T4/T5 短时效与 T1/T6 cv h90）；季节气候态 h30/60/90 产物不含留出段逐行预测，`row_level_degeneracy=null`——与 T4-sug-04（产物 v5 附逐行残差）同源，需训练侧补产物，不在 K3 文件边界内。
2. 后端改动需重启 + 重建快照才在新旧快照上都可见；本轮已通过官方 `POST /model/v3/prediction-snapshot/rebuild` 重建（`...T152435Z_a444f274fdf7`），未改 `CACHE_SCHEMA_VERSION`。后续若纳入正式交付，建议随下一轮训练产物一起升 schema 版本。
3. 本次仅改善可读性，**不提升覆盖率、不关闭校准未通过项**；L-cal-01~07 仍为 FAIL/待验证，交付材料须继续按"未达标能力"表述。
4. 未复跑 `tests/acceptance_e2e.py`（非本轮硬条件）；其 `band_range_blocked.reason` 断言与本次改动兼容（reason 值未变），如需可下一轮补跑。
