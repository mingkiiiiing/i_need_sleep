# T4 解释与不确定性审计报告（2026-09-12）

任务包：T4 解释/不确定性绑定（看板第六节 T4；台账 L-vfy-05）
执行人：T4 解释与不确定性负责人（D 员）
基线：HEAD `d92c421`（磁盘产物 = 运行中 8000 服务加载产物，manifest sha256 `e638e4c4bc43b7a1…` 与台账建账哈希一致；T3b 重训只改训练代码与 experiments_r1，未覆盖 v0_3 serving 包——见"遗留"第 1 条）
方法：全部只读核验 + 独立复算脚本；API 探针 `http://127.0.0.1:8000/api/v1`（8000=A23 后端，未重启未杀）。

---

## 1. 改动/新增文件清单

### 新增（backend/trust_audit/）
| 文件 | 说明 |
| --- | --- |
| `backend/trust_audit/t4_explain_binding_audit_20260912.py` | 模型—解释对应表审计脚本（manifest/bundle/runs 三方对照 + is_shap 语义文本核对） |
| `backend/trust_audit/t4_explain_binding_20260912.json` | 对应表产出（34 模型逐行） |
| `backend/trust_audit/t4_interval_independent_check_20260912.py` | 区间独立检验脚本（逐行覆盖率/宽度/退化复算，serving 同款物理裁剪口径） |
| `backend/trust_audit/t4_interval_check_20260912.json` | 检验产出（29 个带区间模型逐行 + 族共享/同帧复用清单） |
| `backend/trust_audit/T4_解释与不确定性审计报告_20260912.md` | 本报告 |

### 小 bug 修复（改动运行时文件，均通过既有单测）
| 文件 | 改动 | 依据 |
| --- | --- | --- |
| `backend/app/services.py` | 两处 `PREDICTION_SNAPSHOT_NOT_READY`（409）ApiError 显式补 `dataset_version=DATA_VERSION_V3, data_mode="hybrid", claim_boundary=CLAIM_BOUNDARY_V3`（另加对应 import） | 修复前 v3 预测/快照端点的 409 错误响应 meta 为 `data_mode=simulated, dataset_version=DEMO-PRED-V1, claim_boundary=simulation_only`（经 `contracts.dataset_version_for_path` 回落演示轨），与同端点成功响应（`hybrid / real_data_monthly_station_v0_3 / TAIHU_CLEAN_FINAL_V1_20260831`）自相矛盾，且"simulation_only"对 v3 真实模型包是错误声明。属解释/不确定性可信链的口径 bug。复测：`pytest backend/tests -k "snapshot or service or api"` 64 passed（含 test_prediction_snapshot_lifecycle.py、test_v03_api.py） |

注：修复在磁盘代码生效；运行中的 8000 服务为旧代码，需按节奏重启后复核（未重启未杀）。

### 未改代码的发现
无其他可安全小修项；其余问题以建议条目形式列入第 5 节。

---

## 2. 解释绑定结论：**同源成立**

### 2.1 模型—解释对应表（规则 + 证据；逐行见 t4_explain_binding_20260912.json）

34/34 模型全部满足：

| 核对项 | 结果 |
| --- | --- |
| 模型文件存在 + sha256 与 manifest 一致 | 34/34 |
| bundle.run_id == manifest.run_id | 34/34 |
| bundle.selected_family == manifest.selected_family == runs/{run}/selection_manifest.json 选中族 | 34/34（三方一致） |
| 解释方法声明 | `local_one_at_a_time_sensitivity`（局部单因素敏感性，步长=max(±10% 基线, 0.5×训练期σ, 0.01)），`is_shap=False` 恒定（源内仅两处赋值，全为 False；"SHAP"只出现在否定语义说明中） |
| bundle 特征列 | 26 个 cv 模型 8 列、8 个 frozen 模型 9 列（ec37ddf"特征以 bundle 列为准，非 manifest 78 列契约"的既有披露与实物相符） |

### 2.2 同源证据链（代码 + 动态探针双路）

代码链（backend/app/algorithm_models.py）：
1. `predict_suite`/`predict_suite_batch` 内**同一个 bundle 实例、同一 frame** 同时产生：点预测、conformal 区间（`_build_uncertainty`）、解释（`_explain`，缓存键 `(output_key, entity_id, month_offset, horizon_days)`）；
2. `predict_suite` 入口 `_cache_epoch()` 把热点缓存对齐当前实测快照——同一代实测内输入/机理/解释同源；
3. 快照服务（backend/app/prediction_snapshot.py）`assemble` 只从解释缓存**只读**附回（未命中不展示，绝不为读取现算）；`_serving_is_current()` 保证快照与当前模型产物对应，模型产物变化即拒绝服务旧快照；
4. 无 cache-miss→live-inference 路径（backend/app/services.py，409 `PREDICTION_SNAPSHOT_NOT_READY`）。

动态探针（湖面 h7，focus=chla）：
- `results.chla.value = 6.7628024503968325`，`explainability.baseline = 6.762802450396831`——解释基线与点预测**逐位一致**（浮点尾差 1e-15 级），证明解释确实跑在与点值相同的模型输出上；
- `explainability.selected_family = mechanism_feature`，与该时效 bundle/manifest/runs 三方一致；
- run_id 链完整：`source_snapshot_id(mee_surface_water_realtime_20260912T115435Z)` → `prediction_snapshot_id(PRED_…_b6fd7517a0ce)` → 各时效 `prediction_run_id(ALG-V0.3-…-lake-7d)`；解释结果通过 result_key+entity+month_offset+horizon 键与具体一次预测绑定，可追踪。

### 2.3 is_shap=false 语义一致性（API 与前端，只读走查）

- API：解释块 `is_shap=False` + `method=local_one_at_a_time_sensitivity` + note"不是因果贡献，也不是 SHAP 值"；不确定性块 `is_calibrated_confidence_interval=False`（v0.2 层）/三层合同（v0.3 层，见第 3 节）。
- 前端：全仓 `src/` 无任何把敏感性分析称作 SHAP 的文案；仅两处出现"SHAP"且均为否定式免责声明：
  - `src/components/heatmap/LakeDriverDistribution.vue:6`——"机理分解是环境状态口径、与模型无关；它不是模型贡献排序（SHAP）"；
  - `src/components/heatmap/riskAssessment.js:9`——"仅供规则贡献度排序展示，不是 SHAP / 注意力 / 敏感性等模型解释"。
- 解释块呈现（`ForecastResultPanel.vue:336-360`）：`v-if="modelExplanation?.effective"` 才渲染条形排序；解释无效时显示"当前输出对输入扰动没有明显响应"（对应后端 `ineffective_reason`），不把全 0 排序冒充有效解释；特征因子逐项标注 `input_source`（observed / frozen_train_median_baseline），缺测插补不伪装实测。
- 站点视图解释未预热时如实隐藏"AI 响应排序"板块（探针 mee-0145cdb7 快照 explainability=None，前端该板块空态），不拿别的实体/指标解释冒充。

**结论：解释与实际 serving 模型同 bundle 同 frame，同源成立；is_shap=false 在 API 与前端语义一致、诚实。**

---

## 3. 区间独立检验结果

口径：与 serving `_build_uncertainty` 同款物理裁剪（lower=max(x,0)；bloom/coverage/density/probability/spatial 上界 min(x,1)）；**逐行独立复算**（runs/{run}/test_predictions.csv 的逐行预测 × bundle.intervals 残差分位），不复用官方汇总数。

### 3.1 结构有效性（29 个带区间模型；T6 risk_level 5 个 ordinal 无区间，如实 N.A.）
- 原始残差分位 p05≤p95 且有限：29/29；
- 物理裁剪后逐行不倒置：29/29；
- 结构违规：**0**。

### 3.2 覆盖率（可算锚点数 = 测试段逐行数；锚点语义见 3.5）

| 任务/时效组 | 可算锚点 n | 独立复算覆盖率 | manifest 声明 | 差 | 判定（验收线 0.88=0.90−0.02） |
| --- | --- | --- | --- | --- | --- |
| T1 bloom frozen h1/3/7/15 | 40/模型（同帧复用） | 0.975 | 0.975 | 0 | 达标（但见宽度与锚点警示） |
| T6 probability frozen h1/3/7/15 | 40/模型（同帧复用） | 0.975 | 0.975 | 0 | 达标（同上） |
| T3 density cv h1/3/7/15 | 117 | 0.769 | 0.7692 | 0 | 欠覆盖 |
| T3 density cv h90 | 117 | 0.795 | 0.7949 | 0 | 欠覆盖 |
| T4 biomass cv h1/3/7/15 | 117 | 0.744 | 0.7436 | 0 | 欠覆盖 |
| T4 biomass cv h90 | 117 | 0.812 | 0.8120 | 0 | 欠覆盖 |
| T5 chla cv h1/3/7/15 | 132 | 0.803 | 0.8030 | 0 | 欠覆盖 |
| T5 chla cv h90 | 117 | 0.855 | 0.8547 | 0 | 欠覆盖（贴近线） |
| T1/T6 bloom/probability cv h90 | 28 | 1.000 | 1.0 | 0 | 达标（部分行退化，8/28 宽 0） |

- 独立复算 vs manifest 声明：**max diff = 0.0000**（官方覆盖率统计可精确复现，无口径漂移）。
- **真值锚点实况（如实报告）**：可算样本全部为冻结测试段的"标签锚点"，其中——T5 chla 测试段 132 行的标签来源为 `mixed(chla_station_proxy_v1|ground_truth)`；实测来源列 `target_chla_source` 在全面板 914 行中仅 42 行非空（zenodo_taihu_insitu 41 + taihu_water_station_batch 1，T1 审计 L-data-05 所指双单位混用即发生在这 41 行野外数据）；bloom 锚点 `target_bloom_source` 40/40 全空（全代理派生）；density/biomass 无实测来源列（代理契约）。**因此上表覆盖率对 T3/T4 与 T1/T6 是"对代理标签的覆盖率"，不构成对实测真值的覆盖率；实测日/旬尺度锚点不足为 L-data-08 硬阻断。**

### 3.3 区间宽度分布（裁剪后，逐行）
- T1 bloom / T6 probability frozen：宽度中位 **0**、最大 0.017——近零宽（族残差池 p05=−0.351、p95=0，点值贴近 0 时几乎全部宽度被下界裁剪吃掉）；
- T3 density：0.731~0.749（rank 单位）；T4 biomass：21.03~24.46 mg/L（很宽）；T5 chla：9.98~11.17 μg/L。

### 3.4 退化与共享标记
- **完全退化（p05=p95）模型 4 个**：T1 bloom h30/h60、T6 probability h30/60（bundle 残差 [0,0]，校准 n=16）。serving 实际不把这对退化区间直接呈现——30/60 天路由到季节气候态回测残差（`seasonal_climatology_backtest_residual_quantiles`），且单类别测试段（正例 0、负例 31）按 `single_class_test` 处理、`decision_usable=False`、逐条给出"覆盖率不构成判别力证据"原因（API 探针 h30 实测确认）。**退化与单类别处理均如实标记，无冒充。**
- frozen h1/3/7/15 行内 25/40 行宽为 0（贴地裁剪），API 侧对这些行标 `interval_degenerate=False`（未逐行判 0 宽）但整体 `calibration_status=validated`——见第 5 节建议条目 T4-sug-02。
- **族共享残差池**（浮点噪声按 1e-9 归并）：T1 bloom frozen、T6 probability frozen 各自 4 时效共享同一残差池（−0.350995, 0）；T3/T4/T5 cv 短时效各自共享、h90 独立——与 ec37ddf"残差池按任务族共享、非逐时效校准"披露一致。
- **同帧复用独立复核**：T1 bloom 4 行 + T6 probability 4 行共用**同一份** test_predictions.csv（sha16=ef9cd0b74125fdc3，8 行跨任务共用）；T1/T6 cv h30/60/90 两两共用；T3/T4/T5 短时效各自 4 行共用——L-diag-03"同源复用"的独立证据链成立。

### 3.5 30/60 天区间（serving 实际产物）
来源为季节气候态回测残差（evaluation/seasonal_climatology.json，v4 产物）：产物声明覆盖率 T1/T6=1.0（single_class，不开放决策）、T3=0.650、T4=0.761、T5=0.720，均欠覆盖或单类别；产物不含留出段逐行预测，**逐行独立复算被阻断**（如实记录，不替代）。

---

## 4. 地图语义结论（src/ 只读走查）

1. **48/79 口径一致性：通过**。79 站字样已动态化（`ForecastResultPanel.vue` 622-1320 行多处"分母读聚合层实际值，不得写死 79"并落地）；"48"仅存于两处非显示口径：`ForecastResultPanel.vue:653` 注释（历史逐站响应口径说明）与 `Home.vue:150` 首页粒子装饰注释（33/48 可定位站点入画），页面数值分母均来自 `station_aggregate`/快照站点层总数。
2. **场变化来源可区分：通过**。页面三模式互斥（实测回放 / 预测推演 / 遥感年度对比，`Heatmap.vue:587`）；预测站点场仅在预测推演模式渲染（`field-visible="pageMode === 'forecast' && modelFieldEnabled"`）且与右侧结果面板共用同一 `prediction_run_id`（`Heatmap.vue:914` 注释+`loadSpatialField` 实现一致）；观测着色来自 MEE 实测快照、缺测灰点兜底不造值（`Heatmap.vue:526`）；反演统计页注明"全湖像元口径，不等于站点实测"（`Heatmap.vue:302`）。范围带四态 RangeBadge（预测范围/参考范围/情景范围/暂无范围）区分区间语义：全湖聚合 P25–P75 标"站间分布"不冒充预测区间；30/60 天情景锁定走"情景范围"；水华面积（全湖遥感口径）不发站点场请求。
3. **时间轴/快照切换与主结果同源：通过**。预测时效切换为同一快照内存切换（`loadHorizonEvidence` 直读 predictionSnapshot，无请求无模型运行）；实测时间轴用 `/realtime/timeline` 实测快照（与预测轨分离）；未来段分布条用同一份预测快照的全湖聚合中位数（`Heatmap.vue:797`）；驱动分布组件带 `prediction_snapshot_id` 版本守卫（`LakeDriverDistribution.vue:26`），旧世代分布不会覆盖新快照；实测数据/模型版本更新时保留"上一成功预测版本"披露（`Heatmap.vue:23`）。

---

## 5. 建议入台账条目（主控裁定入账）

| 编号建议 | 严重程度 | 明细 | 证据 | 状态建议 |
| --- | --- | --- | --- | --- |
| L-vfy-05（定案更新） | 低 | 解释绑定同源**成立**：34/34 sha256/选族/run_id 三方一致；解释 baseline 与点预测逐位一致；is_shap=false API/前端一致诚实（无冒充） | t4_explain_binding_20260912.json + 本报告 §2 | 待验证 → **PASS** |
| T4-sug-01 | 中 | v3 端点错误出口口径回落演示轨（409 meta= simulated/DEMO-PRED-V1/simulation_only，与成功响应 hybrid/real_data_monthly_station_v0_3 矛盾）——T4 已修 services.py 两处，**待服务重启后 API 复测** | 本报告 §1；修复 diff | 修复待复测 |
| T4-sug-02 | 中 | frozen bloom/probability 区间行内 25/40 宽为 0（贴地裁剪）但 `interval_degenerate=False`、整体 `validated`：行级退化未逐行标记，页面"已核算"字样可能让评委误读区间信息量 | t4_interval_check_20260912.json（T1:bloom:frozen h1 行 width.degenerate_n=25/40） | 待验证 |
| T4-sug-03 | 高（继承） | 覆盖率"达标"（0.975/1.0）建立在代理标签与同帧复用上：T1/T6 frozen 4 时效共用同一 test_predictions.csv（8 行跨任务同帧）；bloom 锚点 40/40 无实测来源。覆盖率证据不能作为"区间可信"的独立支撑对外宣称 | t4_interval_check_20260912.json test_frame_reuse + anchor_source_counts | FAIL（证据弱，非软件错） |
| T4-sug-04 | 低 | 30/60 天气候态区间逐行独立复算被阻断（seasonal_climatology.json 不含留出段逐行预测，仅汇总）；建议产物 v5 附留出段逐行残差 | 本报告 §3.5 | 待验证 |
| T4-sug-05 | 低 | 运行中 8000 服务 manifest/代码与磁盘同版（e638e4c4…），但 T3b 重训未更新 serving 包：算法修复进入 serving 前存在"审计结论针对旧包"的窗口期，需在 T3b 冻结新产物后重跑本审计两脚本并重启服务复核 | manifest generated_at=2026-09-11T10:50 vs T3b commit d92c421 2026-09-12 20:28 | 待验证 |

---

## 6. 遗留与待办

1. **serving 包落后于 T3b 修复**（T4-sug-05）：T3b 修的是训练代码并在 experiments_r1 出了 draft 重训证据，v0_3 serving 包（34 bundle/manifest）未变。T3b 冻结新包后须重跑：`t4_explain_binding_audit_20260912.py`、`t4_interval_independent_check_20260912.py`。
2. **services.py 409 修复待运行时复测**：重启服务后 `curl /api/v1/model/v3/prediction-snapshot`（快照未就绪窗口）应返回 `data_mode=hybrid, claim_boundary=real_data_monthly_station_v0_3`。
3. **真值锚点扩充**：覆盖率对外宣称前必须先解决 L-data-05/08（chla 双单位混用修复 + 日尺度锚点积累）；本审计已给出 42/914 实测锚点基线数。
4. **行级退化标记**（T4-sug-02）与气候态产物逐行披露（T4-sug-04）为后续小改进，涉及 `_build_uncertainty` 与 seasonal_climatology 产物版本，建议归 T4 下一轮或 T3b 冻结时一并处理。
5. 未复跑前端浏览器冒烟（T5 已重跑 70/70，本轮未动 src/）。

—— 审计脚本与 JSON 均在 `backend/trust_audit/`，可重复执行（只读，不触碰运行时）。
