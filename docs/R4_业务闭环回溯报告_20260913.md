# R4 业务闭环专项验收报告（L-vfy-02）：预警→预案→处置→复盘全链 API 级回溯

- 日期：2026-09-13（实测窗口 2026-09-12T17:09~17:15 UTC，下同）
- 验收人：R4 任务包（业务闭环专项）
- 环境：127.0.0.1:8000（A23 后端，product_id=taihu-a23-algae-warning）；8001 禁用未触碰
- 约束遵守：未重启后端、未跑重 CPU 任务、未 git commit/tag；全程只读探针 + 轻量写（以 revoked 事件走合法状态机，收尾恢复原状态，无数据库直改）
- 冻结基线：freeze-r3-20260913（HEAD 1323c78）

---

## 一、走查的端点清单（来源 OpenAPI /openapi.json）

| # | 方法 | 端点 | 用途 | 本次是否实测 |
| --- | --- | --- | --- | --- |
| 1 | GET | /api/v1/realtime/alerts/overview | 引擎告警总览（活跃告警/投递状态） | 是 |
| 2 | GET | /api/v1/realtime/alerts | 告警事件历史 | 否（overview 已覆盖） |
| 3 | POST | /api/v1/realtime/alerts/evaluate | 手动巡检（引擎评估+中心差分同步） | 是×2 |
| 4 | GET | /api/v1/realtime/alerts/center | 预警中心总览（事件/预案/规则/通知/未读数） | 是 |
| 5 | GET | /api/v1/realtime/alerts/center/events/{id} | 事件详情（证据链/记录/任务/回执/推荐预案） | 是 |
| 6 | POST | /api/v1/realtime/alerts/center/events/{id}/actions | 工作流动作（状态机） | 是×9 |
| 7 | POST | /api/v1/realtime/alerts/center/events/{id}/plan | 采纳预案 | 是（成功1+被拒1） |
| 8 | POST | /api/v1/realtime/alerts/center/events/{id}/push | 模拟推送 | 是 |
| 9 | POST | /api/v1/realtime/alerts/center/events/{id}/tasks/{tid} | 任务进展 | 否（未推进任务，保持最小写入） |
| 10 | GET | /api/v1/realtime/alerts/center/notifications | 站内通知/未读 | 是 |
| 11 | POST | /api/v1/realtime/alerts/center/notifications/read | 标记已读 | 否（避免改实库已读状态） |
| 12 | GET | /api/v1/realtime/alerts/center/records | 处理记录/推送/审计 | 是 |
| 13 | POST | /api/v1/realtime/alerts/center/rules | 规则更新 | 否（不改实库规则） |
| 14 | GET | /api/v1/history/reviews | 复盘列表 | 是 |
| 15 | GET | /api/v1/history/reviews/{id} | 复盘详情 | 是 |
| 16 | GET | /api/v1/history/reviews/{id}/evidence | 复盘证据轨 | 是 |
| 17 | GET | /api/v1/history/reviews/{id}/timeline | 复盘时间线 | 是×2 |
| 18 | PUT | /api/v1/history/reviews/{id}/review-notes | 复盘笔记 | 否（不改实库） |
| 19 | GET | /api/v1/history/metrics | 历史指标 | 是 |

关键结构结论：**系统无手工"建单"端点**。处置事件由 `evaluate`（引擎评估→`AlertCenter.sync_from_engine` 差分同步）自动创建/升级/恢复，幂等防线在该层。

## 二、基线快照

- 引擎：3 条活跃告警（新港口 moderate 97.6 / 拖山 light 12.67 / 锡东水厂 light 10.6 μg/L），真实通道 enabled=false，投递均为 `push_disabled: alerts-config.json enabled=false（评估照常，推送未启用）`
- 中心：8 事件贯穿状态机（review×4、closed×3、revoked×1、pending×0），未读通知 52；后台巡检线程 300s 运行中（日志确认 `interval=300.0s enabled=False`）
- 预案库 3 套（轻度/中度/预测准备），auto_simulate_push=false

## 三、全链步骤结果表

| 步骤 | 操作（API 级） | 结果 | 关键证据字段 | 判定 |
| --- | --- | --- | --- | --- |
| 1 巡检触发 | POST /realtime/alerts/evaluate ×1 | 200；79 站扫描、warnings_seen=3、triggered/escalated/resolved 均空（3 活跃告警均有对应事件） | `evaluated_at`、`snapshot_id=mee_surface_water_realtime_20260912T155436Z` | PASS |
| 2 建单（自动） | evaluate 差分同步 + GET events/{id} | 活跃告警均有唯一事件；未关闭事件经 `engine_alert_ids` 并入同一事件（拖山 9 个引擎告警→1 事件；急水港桥 10→1） | 证据链：`indicator=叶绿素a, value=10.03, threshold=10.0, observed_at, triggered_at, snapshot_id, state=valid, rule_name=蓝藻筛查·黄色预警（叶绿素a ≥ 10 μg/L）` | PASS |
| 3 幂等验证 | **同一告警重复触发建单**：evaluate 连续两次 | 两次事件集合 diff 为空、事件数恒 8、unread 恒 52；无重复建单。升级复用路径另证：同站新告警并入未关闭事件而非新建（alert_center.py L366-404） | 第2次 evaluate 返回 `triggered=[]`，事件数 8→8 | **PASS（方案 5.B 核心要求达成）** |
| 4a 预案匹配 | GET events/{id} → matched_plan | 推荐 `plan-light-realtime`（轻度关注预案 v1.0），带 4 条匹配依据 | `match_reasons=[处置阶段一致, 风险等级适用（黄色预警）, 风险类型一致, 适用范围覆盖事件对象]` + 3 条 measures | PASS |
| 4b 采纳约束 | review 状态 POST plan | **422**："仅在已确认/处理中状态可采用预案" | HTTP 422 + 可读原因 | PASS |
| 4c 采纳动作 | reopen→processing 后 POST plan | 200；生成 3 项措施任务 | `plan.adopted_at`、`tasks×3(todo)`、records `adopt_plan` 留痕 | PASS |
| 5a 处置推进 | revoke 事件 reopen（唯一合法入口）→ processing | 200 | records `reopen`，closed_at 清空 | PASS |
| 5b 指派（最轻步骤） | POST actions assign | 200；assignee=R4验收-处置组 | records+audit 留痕；站内通知 +1 | PASS |
| 5c 持久化复查 | 再次 GET events/{id} | status/assignee/plan 均与写入一致（reopen 后复查 processing、assign 后复查留痕） | 两次独立 GET 交叉确认 | PASS |
| 5d 非法跳步约束 | processing 发 close / start；review 发 confirm；未知动作 | close→422"当前状态不允许（处理中）"；start→422；confirm→422"（待复核）"；bogus→422"unknown action" | 全部 422 且带状态标签 | PASS |
| 5e 关闭必须人工 | 实证：evt-baa0ff13a869 evidence=recovered 自 09-12T17:06 起保持 review，历经 2 次手动 evaluate + 后台 300s 巡检多轮，未被自动 close | 恢复只转待复核（alert_center.py L450-456），关闭仅 `close` 动作且仅 review 可触发 | status=review 持续 + sync 代码路径 | PASS |
| 5f 缺失≠恢复 | 代码层：resolve_reason=station_missing → evidence.state=unverified、通知"数据待核实（不视为恢复）"、不转 review（L437-444） | **实库无 unverified 运行时实例**（8 事件均未经历 station_missing），API 级只能证到代码路径与导入分流逻辑（L286-289） | 无反例（无"缺失被当作恢复"事件） | PASS（附运行时无实例观察项 O-3） |
| 6 模拟推送 | POST push {email+sms, 监测组+处置组} | 200；status 均 `simulated_success`，回执 `SIM-2F0F9705`/`SIM-5501FBD7` | 收件人 `di***@a23-sim.invalid`、`mo***@a23-sim.invalid`、`138****001/003`（脱敏）；reason 明确"**未实际发送**邮件/短信；收件人来源：模拟收件人注册表"；正文尾"（模拟消息，不实际发送）"；audit `simulate_push` 留痕 | PASS |
| 7a 复盘列表 | GET /history/reviews | 8 事件在列，by_status 与中心一致；目标事件可检索 | `stats.by_status` 与中心 overview 对账一致 | PASS |
| 7b 复盘详情 | GET /history/reviews/{id} | records 视图含本次全步（reopen/adopt_plan/assign/simulate_push/submit_review）；`links.alert_center_event` 反向链回中心事件、`station_observations` 链回原始观测 | 12 条 records + related_links | PASS |
| 7c 复盘时间线 | GET …/timeline | 16 节点全链可追：import→**trigger（叶绿素a 10.5 ≥ 10.0，规则名）**→历史处置（reopen×2/assign/notify/submit_review/close/revoke）→本次 R4 全步（含 SIM 回执号）；notification 节点标注"铃铛通知未读；已读≠已确认事件" | 里程碑含原始告警与处置每步 | PASS |
| 7d 复盘证据 | GET …/evidence | 观测轨窗口"首次触发前 24h → 事件结束后 2h"，五指标（叶绿素a/水温/总磷/总氮/溶解氧），available=true | window.basis 口径明确 | PASS |
| 7e 存量 closed 事件复盘 | evt-8b6a406f08b6 timeline | 仅 2 节点（import+trigger），无关闭动作节点；`response_metrics.valid_recovery=false` 但 evidence_state=recovered | bootstrap 批量导入的 closed 事件缺人工 close/recovered 动作留痕 | **不符（O-4，存量导入数据的链路缺口，非本次新增）** |
| 8 通知计数 | 全程对比 unread | 52→53，唯一增量=assign 的 process 通知（notify.process=true）；reopen/adopt/close/revoke/推送均不产通知，符合"一条变化只产生一条未读" | 类型分布 process=2（历史1+本次1） | PASS |
| 10 收尾 | review 状态 POST revoke（合法） | 200；事件恢复初始 **revoked**；assignee/plan/records/pushes 按审计要求留痕且均标注"R4验收" | 终态快照 8 事件与基线状态逐一相同（唯一差异：AE7C records 13 条、未读 53、audit 6 条 R4 留痕） | PASS |
| 附：submit_review 合法流转 | processing POST submit_review（复现） | 200，processing→review 落盘成功（后由 revoke 收尾） | audit 17:11:54 成功 | PASS（但见 F-1） |

## 四、不符 / FAIL 候选清单

| 编号 | 级别 | 现象 | 实证 | 根因定位 |
| --- | --- | --- | --- | --- |
| **F-1** | **FAIL 候选（中置信，建议修复）** | 处置动作**非原子落盘**：首次 `submit_review` 返回 HTTP 500"服务内部错误"，但 audit.json 中 17:10:44 已写入该动作"成功"记录，而事件 records/状态**未变更**（17:11:54 前 GET 仍 processing）。即出现"审计说成功、业务未生效"的不一致窗口 | audit 17:10:44 submit_review 成功 vs 事件 records 无对应条目；客户端 500；同请求 3 分钟后复现 200 | `alert_center.py apply_action`（L595-643）：先 `_audit`（L641）后 `store.save_events`（L642），两个文件无事务；疑为 Windows 下 `temp.replace` 文件占用瞬态失败。日志 uvicorn.out/err 无请求级堆栈，500 根因无法从日志确证（见 O-1） |
| F-2 | 低危瑕疵 | 升级留痕文案自相矛盾："风险等级由 黄色预警 升级为 黄色预警"（同级也记 escalate） | evt-82e1a073db82 records 17:09-10T04:27:49 | `sync_from_engine` L395-400 未比对 old_level≠new_level |
| O-1 | 观察项 | 500 无堆栈可查：uvicorn 日志仅启动信息，无请求级错误记录 | uvicorn.out/err.log 共 ~1.8KB | 建议全局 exception handler 记录 traceback |
| O-2 | 观察项（代码级隐患） | `apply_action` L599-600 抛 `KeyError`，`action_or_404` 只捕 `ValueError`→422；若检查与操作间事件消失会 500（TOCTOU，当前无删除 API，触发面小） | 代码审读 | 建议改抛 CenterNotFound |
| O-3 | 观察项 | "缺失≠恢复"约束无运行时实例（8 事件均未经历 station_missing），本轮仅能验证代码路径与无反例 | center records 无 unverified 动作 | 建议后续以受控 fixture 补一条端到端验证 |
| O-4 | 不符（存量数据） | bootstrap 批量导入的 closed 事件复盘链不完整：timeline 仅 import+trigger，无人工关闭节点；`valid_recovery=false` 与 `evidence_state=recovered` 口径不一致 | evt-8b6a406f08b6 timeline 2 节点 | `_import_engine_alerts` 未为导入态补 records/recovered_at；属导入捷径特性，建议复盘页加"批量导入"披露 |

## 五、建议入台账条目（第三轮台账 C 组追加）

| 条目 | 内容 | 状态建议 |
| --- | --- | --- |
| L-vfy-02 | 业务闭环全链回溯：19 端点走查、幂等/持久化/状态机约束/模拟推送/复盘回溯全部 PASS（见本报告第三节） | **待验证→PASS**（幂等与"缺失≠恢复、关闭必须人工"两项核心约束均有实证；F-1 不阻断链路结论） |
| L-r4-01（新） | F-1：apply_action 审计与事件落盘非原子，实测产生 audit 成功/事件未变的 500 不一致窗口；建议补事务顺序（先事件后审计）或失败回滚审计 | FAIL（修复待复测） |
| L-r4-02（新） | F-2：同级升级误记 escalate（"黄色→黄色"）文案瑕疵 | 待修复（低危） |
| L-r4-03（新） | O-4：批量导入 closed 事件复盘链缺口 + valid_recovery 口径不一致 | 待验证（存量披露即可） |

## 六、遗留

1. F-1 根因需服务端日志支撑：当前 uvicorn 无请求级堆栈，建议先加 traceback 日志再复测（本轮禁重启后端，未实施）。
2. O-3"缺失≠恢复"端到端实例验证：需构造 station_missing fixture，受当前实库数据限制未做，代码路径已审读无反例。
3. 未实测端点 4 个（tasks 推进、notifications/read、rules、review-notes PUT）：均为写操作且会改实库观感/规则，本轮按最小写入原则跳过；其代码路径已审读（schemas 校验完整）。
4. 测试留痕说明：evt-9ff68fc5cc72（AL20260908-AE7C）已恢复初始 revoked 状态；留有 R4 标注 records 6 条、模拟回执 2 条、audit 6 条、1 条未读 process 通知（assign 产生）——均为系统合法操作的审计留痕，未做数据库直改，如需清理可走后续人工复核口径。

## 七、测试数据收尾核对（终态 vs 基线）

| 事件 | 基线状态 | 终态状态 | 一致 |
| --- | --- | --- | --- |
| AL20260909-F116 急水港桥 | review/recovered | review/recovered | ✓ |
| AL20260908-F324 拖山 | review/valid | review/valid | ✓ |
| AL20260908-CADB 拖山 | closed/valid | closed/valid | ✓ |
| AL20260908-AE7C 急水港桥 | revoked/recovered | **revoked**/recovered | ✓（验收对象，留痕见上） |
| AL20260908-5913 急水港桥 | closed/recovered | closed/recovered | ✓ |
| AL20260908-5CA9 急水港桥 | closed/recovered | closed/recovered | ✓ |
| AL20260908-278F 锡东水厂 | review/valid | review/valid | ✓ |
| AL20260908-E242 新港口 | review/valid | review/valid | ✓ |

未读通知 52→53（+1 为 assign 站内通知，合理）；引擎 3 活跃告警无变化。
