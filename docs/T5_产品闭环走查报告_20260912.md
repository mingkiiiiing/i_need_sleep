# T5 前端产品闭环走查报告（2026-09-12）

任务包：T5 前端产品闭环（F 员）。依据《零FAIL并行开发任务看板_20260912.md》第六节 T5、第三节文件边界；《零FAIL台账_20260912.md》D 组 L-vfy-01、L-vfy-06。
本报告只做走查、一致性对照、测试复跑与文案级小修；未改任何后端文件，未 commit/push/tag，未改台账与看板。

## 0. 环境探活（开工与收工各一次）

| 服务 | 结果 |
| --- | --- |
| 后端 8000 `/api/health` | 200，product_id=taihu-a23-algae-warning，api 2.2.0，直接使用未重启 |
| 前端 5173（vite） | 200，直接使用未重启 |
| 8001 | 未触碰（他项目占用） |
| observed 轨实况 | `/realtime/status`：data_mode=observed、MEE-RT-V1、79 站、最新观测 2026-09-12T19:00+08:00、freshness=normal、快照 217 份 |
| 预测快照实况 | `/model/v3/prediction-status`：state=ready、schema v11、model_version 0.3、feature_contract 78 特征（与 manifest sha256 一致） |

## 1. 产品主流程代码级走查表（8 环节）

主流程：发现需关注位置 → 查看最新观测和历史 → 指定时效研判 → 主要驱动与可信程度 → 创建预警 → 匹配预案 → 记录处置 → 复盘。
导航入口：侧边栏 7 项（`src/components/common/AppSidebar.vue`）+ 首页六入口卡（`src/pages/Home.vue` EntryGrid，顺序=业务动线）+ hash 路由（`src/main.js`，createWebHashHistory）。

| # | 环节 | 页面/路由 | 关键组件 | 状态 | 信息完备性（观测时间/过期/代理性质/时效口径） | 断点与误导风险 |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | 发现需关注位置 | `/#/cockpit`（首页 `/#/` 亦有实时态势+SystemPulse 计数） | LakeMap、TopStatusChip（error/historical/abnormal/normal 四态）、WarnCard、HealthCard、KpiCard、ReplayTimeline | 走通 | 点位图例含 10/25 μg/L 筛查口径；异常态显示"观测时间+滞后时长"；失败态明示"不会回退情景数据"；历史快照回放不冒充实时（is_latest 才 setRealtimeUpdate） | 无断链。WarnCard 点击→focusStationById→站点抽屉，入口可达 |
| 2 | 查看最新观测和历史 | `/#/stations?p=<id>`（首页站点热区/驾驶舱抽屉/预警中心"查看监测站"链接均可深链） | StationListPanel、LakeMap、StationTrendPanel、StationProfileCard、StationObsCard、StationAlertsCard、StationForecastCard、StationDetailTabs | 走通 | 最新观测卡显示 observed_at 与 11 项指标（缺测/质控不合格如实标注）；stationDataStatus 按 ≤6h/≤12h/>12h 分带（正常/延迟/严重过期），列表按数据状态排序；质量与覆盖率拆散就近展示 | 无断链。站点级预测（/realtime/stations/{id}/forecast）当前数据窗口 MEE 上游 chla 全缺测（upstream_missing）→ 全部 409，页面渲染 uncovered 三态如实说明"本站暂无模型预测……仅覆盖有叶绿素 a 序列的站点"，不以规则结果冒充模型预测（数据现状，非前端断链） |
| 3 | 指定时效研判 | `/#/heatmap?mode=forecast&scale=short|mid|long&metric=risk|chla|area|biomass&station=&stop=t1..t90` | ForecastControlPanel、ForecastTimeline、RsMap、RasterLayer、ForecastResultPanel、predictionSnapshot store | 走通 | T+1…T+90 七时效装在同一份预生成快照（prediction_snapshot_id 全链绑定，smoke T1b 验证）；时间轴"短临 1-3 / 趋势 7-15 / 中长期 30/60/90 月度"；短期四档同月标签合并标注"同月短期结果"（对齐 manifest granularity_disclosure）；预测起报时间/数据更新时间在左栏"推演时间" | 无断链。URL 深链/前进后退/快速切换均不串站（smoke T2/T3/T6/T7）；快照未就绪走 pending 重试，读取接口零回落推理 |
| 4 | 主要驱动与可信程度 | 同上页"驱动因素/不确定性"两标签 + 主视图 RangeBadge + 可展开 EvaluationPanel | LakeDriverDistribution、HorizonFanChart、EvaluationPanel、RangeBadge、intervalState/focusEvaluation（store） | 走通 | 驱动因素逐项标"逐站/全湖"徽标与来源（本站实测·MEE 水温 / ERA5 网格 / 代理输入 / 缺少有效数据），光照上界明示"已取到上限"；区间按后端三层合同（结构自洽→校准证据→决策可用）判定，覆盖率环未核算时明示"未核算"；EvaluationPanel gate_status 原样展示 PASS/FAIL/NA（na_reason 附中性解释）；门禁 0 PASS 下页面无正面"可用于站点比较"（smoke T1 断言） | 无断链。页面无任何 SHAP 声明："AI 响应排序"明示"输入变化对当前输出的影响"（输入扰动敏感性口径），riskAssessment.js 头注明示"不是 SHAP/注意力/敏感性" |
| 5 | 创建预警 | `/#/alerts`（首页/侧边栏/铃铛 `?event=` 深链） | Alerts.vue + alertCenter.js + alerts.js（evaluateAlerts 巡检） | 走通（实时轨） | 事件详情含当前值/触发阈值/观测时间/触发时间/证据状态（有效/已恢复/待核实）/触发规则；"立即巡检"按钮显式触发 | 产品断点（如实披露）：预测预警事件生成链路未接入——预测 tab 空态文案"预测预警尚未接入……当前不伪造预测数据"。规则配置中 predicted 规则已可设置门槛但 enabled=false（后端 predicted_rule_enabled=false），属待联调项，无误导 |
| 6 | 匹配预案 | 同上页右侧栏 | Alerts.vue（matched_plan/plan） | 走通 | 推荐预案显示匹配依据（match_reasons 逐条）与建议措施分项；采用前必须先确认预警（canAdoptPlan 限定 acknowledged/processing）；采用后任务独立留存（预案库更新不影响已生成任务，页面有明示） | 无断链。预案库 3 个（轻度/中度/预测准备），与事件类型匹配 |
| 7 | 记录处置 | 同上页详情/底部记录区 | Alerts.vue（STATUS_ACTIONS 状态机、任务清单、模拟推送弹窗、records 三 tab） | 走通 | 处置流程五步（待确认→已确认→处理中→待复核→已关闭，可撤销/重开）；任务 todo/doing/done 逐项更新留痕；模拟推送明示"不实际发送短信/邮件"、收件人脱敏、回执单列 | 无断链。实库验证：8 个事件（1 处理中/3 待复核/3 已关闭/1 已撤销）、平均响应 784.3 分钟（样本 2）真实留痕 |
| 8 | 复盘 | `/#/history?view=review|replay|evaluation` | EventReviewView、ObservationReplayView、PredictionEvalView、ReviewConclusion、ReviewTimeline、historyReview.js | 走通（实时轨） | 事件复盘以 event_id 为主键串联证据链（观测/触发/处理/预案/推送六类节点着色区分）；结论区口径明示"任务完成≠措施被证明有效；模拟推送成功≠真实送达；待核实不视为恢复"；观测回放明示"缺测不插值，不做模拟补齐"；复盘报告可导出 Markdown | 实时轨闭环走通（8 事件可复盘）。"预测评估"视图后端返回 available=false、capability_state=awaiting_official_forecast，页面如实转述，不编造准确率 |

结论：主流程 8 环节页面全部存在、入口全部可达、实时轨闭环走通且信息贴近结果展示；未发现"待接入/未接入"类披露 UI 被重新加回时空推演页（smoke T10b 有回归断言防守）。

## 2. 声明一致性对照（页面 vs backend/model_runtime_v0_3/manifest.json + gate_table + 台账权威口径）

后端事实基准（只读核对）：
- manifest：claim_boundary=real_data_monthly_station_v0_3、34 个模型、granularity_disclosure="月度标签粒度：1/3/7/15 天映射为当月标签（7/15 为半月近似披露），30/60/90 天映射为 t+1/2/3 月"。
- gate_table 42 行 = 24 FAIL / 12 NA / 6 PASS（与台账 A/B 组逐条一致）。
- 运行时快照（2026-09-12 实测）：h1 probability entity_diagnostic gate_status=FAIL、model_family=random_forest、artifact_id=T6:probability:frozen_split:off0:h1:s20260907；h30=NA+seasonal_climatology_baseline；metric_diagnostics 与快照三态诊断齐全。

| # | 页面声明位置 | 页面表述 | 权威口径 | 结论 |
| --- | --- | --- | --- | --- |
| 1 | Heatmap modelInfo（左栏） | "算法交付包 V0.3（真实数据 · N 模型）"，N 读 `/model/v3/status` model_count 动态 | manifest models=34 | 一致（动态） |
| 2 | Heatmap modelInfo legacy 分支 | "算法交付包 V0.2（63 模型）" | backend/model_runtime_v0_2/models 实数 63 个 joblib | 一致（硬编码但数值正确，仅 V0.3 不可用时显示） |
| 3 | 快照三态诊断→EvaluationPanel/来源披露 | model_family/model_run_id/model_file/artifact_id 原样透传 | manifest selected_family（T6-probability=random_forest、T3-density=residual、T4-biomass=constrained_blend、T5-chla=mechanism_feature 等） | 一致（运行时实测比对 h1 一致） |
| 4 | T+30/60 来源披露 | "季节气候态基线：按月给出历史同期值，全湖同值、不含站点分辨（该时效原模型留出样本不足，已按路由规则降级）" | manifest 中 T1/T6 h30 selected_family=persistence（serving 实际走 seasonal_climatology_baseline 路由降级，entity_diagnostic 如实回传） | 一致（页面按 serving 事实表述并披露降级原因，未谎称 persistence 模型输出） |
| 5 | T+90 来源披露 | 逐站响应才称"逐站模型"+留出集 MAE/R²/n+模型文件名；无响应则称"全湖常量模型（读模型文件但无站点响应）" | T5-chla-3m=random_forest 逐站、T1/T6-probability/T4/T3-3m=climatology_global 常量 | 一致（2026-09-12 复审整改措辞已在代码中落实） |
| 6 | 趋势图注 | "短期四档（T+1/3/7/15）共用同一个月度标签，数值按定义相同……不含日尺度信号"；中长期虚线分口 | manifest granularity_disclosure / horizon_month_map {1:0,3:0,7:0,15:0,30:1,60:2,90:3} | 一致（逐字对齐） |
| 7 | EvaluationPanel 门禁徽标 | gate_status 原样 PASS/FAIL/NA 不改写；na_reason=test_n_below_minimum_15 附中性解释"不视为通过" | gate_table 24 FAIL/12 NA/6 PASS | 一致（运行时 h1=FAIL 如实上页面） |
| 8 | 覆盖披露三行（左栏） | 预测覆盖 done/total（动态）、地图覆盖 plottable/total=48/79、"未上图 N 站：缺少可核验坐标，仅列表查看" | 台账 48/79 上图口径；运行时 spatial-field coverage total=79/with_prediction=79/plottable=48 | 一致（smoke T9 断言动态分母+48/79） |
| 9 | 站点页"最新预测"卡标签 | "机理+AI 融合 v0.1"；试点评估（时间交叉验证）MAE/基线/档位准确率 | backend/app/station_forecast.py MODEL_FAMILY=mechanism_ai_fusion_station_v0.1、cascade(0.4×机理+0.6×AI)+经验残差区间 | 一致 |
| 10 | SHAP 相关 | 全站无 SHAP 字样声明；"AI 响应排序（输入变化对当前输出的影响）"“规则贡献度排序，不是 SHAP/注意力/敏感性” | 台账 L-vfy-05：is_shap=false 不得改名 SHAP | 一致 |
| 11 | 大屏 Wallboard 免责 | "官方观测未经跨源验证，不用于监管决策" | `/realtime/*` meta.claim_boundary=official_observation_not_cross_validated | 一致 |
| 12 | 台账"7 站 T+1"站点预测试点 | 页面如实三态；当前窗口 MEE 上游 chla 全缺测 → 抽测 7 站全部 409 → uncovered 态"本站暂无模型预测" | 台账口径为能力上限；当前数据窗口实际可预测 0 站 | 一致（页面不夸大；数据现状记入建议条目） |
| 13 | **不一致（已修）** | ForecastResultPanel.vue 渲染文案 9 处、LakeDriverDistribution.vue 3 处硬编码"79 站" | 组件自身整改注释（2026-09-12）："所有'79 站'字样必须读实际聚合分母，禁止写死"；运行时 station_total=79（目录 79、当轮活跃可能 78） | **不一致→已改动态分母**（见 §4 修改清单） |
| 14 | 遗留观察（未改） | `src/data/dataIdentity.js` 全文件为旧"全情景数据/非决策用途"口径（claimNote/provenance 均为 SCENARIO 时代文案） | 现状为 observed/simulated 双轨；该文件仅剩 lakeName 被 Home.vue 引用，声明字段无消费方，不构成页面误导 | 建议条目 S2（终审前清理，防止未来误引用） |

## 3. 两项测试复跑结果（2026-09-12，T5 会话内真重跑）

| 测试 | 命令 | 结果 | 备注 |
| --- | --- | --- | --- |
| 前端逻辑测试（L-vfy-06 前端半） | `npm.cmd test`（node --test tests/*.test.mjs） | **12/12 通过**（pass 12 / fail 0 / duration 0.51s） | 小修前跑一次 12/12；小修后回归再跑一次 12/12。用例覆盖预测快照竞态/缓存世代/三态诊断不放宽/test_n=1 不显示校准有效等 |
| 浏览器冒烟（L-vfy-01） | `npm.cmd run test:browser`（puppeteer-core + Edge headless，5173/8000 健康下） | **70/70 通过**（修前跑一次 70/70；小修后回归 70/70） | 控制台零错误零警告；四区域绑定同一 prediction_snapshot_id；48/79 覆盖披露、门禁 0 PASS 无正面"可用于站点比较"、区间三层合同双向断言全过。旧 70/70 报告已由本次运行覆盖（tests/browser-smoke-report.json 更新为本轮结果） |

## 4. 修改清单（本次全部改动，均通过两项测试回归）

| 文件 | 改动 | 性质 |
| --- | --- | --- |
| `src/components/heatmap/ForecastResultPanel.vue` | 渲染文案 9 处硬编码"79 站"改为动态 `${lakeStationCount.value ?? '—'} 站`：①horizonTrendTitle（页面直接渲染的"79 站中位趋势"标题）；②heroBar chla/biomass 两处 title；③趋势图逐点 hover title（lake 分支）；④lakeCards 五张聚合指标卡 title（概率/叶绿素/生物量/密度/全湖风险等级） | 口径对齐小修（组件 2026-09-12 整改注释"禁止写死"的执行残留） |
| `src/components/heatmap/LakeDriverDistribution.vue` | 3 处：标题"全湖驱动因素 · {{meta.nStations}} 站分布"、加载态文案去掉写死数字、净生长率副标题"（{{meta.nStations}} 站分布）" | 同上 |
| `tests/browser-smoke-report.json` | 测试脚本自动写入的本轮 70/70 运行结果 | 测试产物（非手改） |

未改动：后端全部（只读）；时空推演页未新增任何披露 UI；测试断言未放宽。

## 5. 建议入台账条目（供主控入账）

| 编号建议 | 类型 | 内容 | 状态建议 |
| --- | --- | --- | --- |
| L-vfy-01 定案 | 复测 | 浏览器功能冒烟 2026-09-12 真重跑 70/70 通过（puppeteer-core+Edge headless，5173/8000 健康窗口；报告 tests/browser-smoke-report.json 为本轮产物，控制台零错误零警告） | 待验证 → PASS |
| L-vfy-06（前端半）定案 | 复测 | 前端逻辑测试 2026-09-12 复跑 12/12 通过（npm.cmd test）；"后端可信 40/40"半项仍待阶段3 统一复跑 | 部分 → 前端 PASS |
| L-t5-01（新） | 修复 | 时空推演页 12 处"79 站"硬编码改动态聚合分母（ForecastResultPanel 9 处+LakeDriverDistribution 3 处），与 2026-09-12"站点分母动态化"整改要求对齐；修后 12/12+70/70 回归通过 | PASS |
| L-t5-02（新） | 待验证 | 预警中心"预测"事件生成链路未接入：predicted 规则 enabled=false，预测 tab 空态如实披露"当前不伪造预测数据"；建议阶段3 联调打通 prediction-snapshot→predicted 事件（对应 L-vfy-02 预测半） | 待验证 |
| L-t5-03（新） | 待验证 | 站点级预测试点（台账口径 7 站 T+1）当前数据窗口 MEE 上游叶绿素 a 全缺测（upstream_missing），实测 7 站请求全部 409 → 页面 uncovered 如实说明；能力存在但无数据可预测，建议数据侧跟进上游 chla 可用性 | 待验证 |
| L-t5-04（新） | 建议 | `src/data/dataIdentity.js` 为早期"全站情景数据"时代遗留口径（claimNote/provenance 等），现无页面消费（仅 lakeName 被 Home 引用）；建议 T6 终审前清理或改写为双轨口径，防未来误引用造成全站声明倒退 | 建议 |

## 6. 遗留与待办

1. 阶段3 联调项：预测预警事件链路（L-t5-02）、复盘"预测评估"视图数据源（后端 awaiting_official_forecast，依赖正式预测接入）。
2. 站点级预测试点的实际效果依赖上游 chla 报数（L-t5-03），建议在 MEE 恢复 chla 报数窗口复测站点预测卡的 ok 态（模型卡披露/区间/驱动条均已在代码中实现）。
3. T5 交接物"无提示试用脚本与记录表"未在本任务包内产出（看板 T5 三项之一），建议随阶段3 产品评委评审一并准备；本报告走查表可直接作为试用路径底稿。
4. 本轮小修未 commit（纪律：禁止 commit）；建议主控以 `T5: 时空推演页 79 站硬编码改动态分母+走查报告` 为消息独立提交。
5. 复盘页 EventReviewView 等 7 个子组件按声明文案与服务层口径核对（grep 全量+关键文件阅读），未逐行审查渲染逻辑；如需逐行深度以阶段3 交叉审查为准。
