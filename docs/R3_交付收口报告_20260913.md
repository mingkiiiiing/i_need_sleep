# R3 交付收口报告（2026-09-13）

任务包：R3 交付收口。依据：《遗留清零与冻结重跑看板_20260913.md》任务包 R3、《遗留清零与冻结重跑台账_20260913.md》A 组 L-vfy-07/09、L-t5-04/05。
红线遵守：重训期间未跑重 CPU 任务、未重启 8000 后端、未 git commit/tag（改动均留工作区由主控统一提交）。
冻结基线：git tag `freeze-r3-20260913`（HEAD 1323c78）。

## 一、改动文件清单

| # | 文件 | 动作 | 说明 |
| --- | --- | --- | --- |
| 1 | `企业提交材料/A23_环境与部署说明_V1.0.docx` | 修改（python-docx，格式保留） | L-vfy-09：4173/5173 口径统一 + 8000 换端口说明，共 7 处（详见第二节） |
| 2 | `_backups/A23_环境与部署说明_V1.0_修改前_20260913.docx` | 新增 | 修改前原样备份（48,465 字节，与既有 20260912 备份并存） |
| 3 | `src/data/dataIdentity.js` | 重写精简 | L-t5-04：删除全部旧"全情景数据"口径字段，仅保留被引用的 lakeName + deprecated 说明 |
| 4 | `tests/试用评审_无提示试用记录表_20260913.md` | 新增 | L-t5-05：主持指南 + 8 张任务卡 + 3 份空白记录表 + 汇总口径 + 结果回填节 |
| 5 | `docs/R3_交付收口报告_20260913.md` | 新增 | 本报告 |

未改动：后端全部、git 历史（无 commit/tag）、.gitignore、台账与看板（主控维护）。

## 二、L-vfy-09 残余：部署说明 docx 修订

**做了什么**：用 backend/.venv 中的 python-docx 对《A23_环境与部署说明_V1.0.docx》做 7 处修改，全部单 run 原位替换（保留原样式字体），文档可正常重新打开（87 段）：

| # | 位置 | 前口径 | 后口径 |
| --- | --- | --- | --- |
| 1 | §5.1 步骤 4 | 未自动打开访问 `http://127.0.0.1:4173` | 改为 `http://127.0.0.1:5173`（与 start-a23-dev.ps1 dev 口径一致） |
| 2 | §5.1 新增段落（步骤 4 后） | （无） | 补充：8000 被占用时 `powershell -ExecutionPolicy Bypass -File start-a23-dev.ps1 -BackendPort 8001`，后端监听 8001、脚本自动把前端代理指向 8001、前端仍为 5173 |
| 3 | §5.2 正文 | "比赛演示优先使用构建后的预览模式"（即演示→4173 预览） | "一键启动与比赛演示统一使用 vite 开发模式（npm run dev），访问 5173，与 start-a23-dev.ps1 一致；npm run build/preview（4173）仅可选验证构建产物，不作为演示与验收入口" |
| 4 | §2.1 端口要求表 | 5173 行说明"开发调试使用"；4173 行说明"比赛演示推荐" | 5173="一键启动与演示使用"；4173="仅可选验证构建产物" |
| 5 | §6.1 部署验证表 | 前端首页检查地址 `http://127.0.0.1:4173` | 改为 `http://127.0.0.1:5173` |
| 6 | §7 常见问题表"8000 端口被占用"行 | "关闭旧进程或统一修改后端与代理端口" | "关闭旧进程；或按 5.1 使用 -BackendPort 8001 换端口启动" |
| 7 | §10 版本说明后 | （无） | 追加"2026-09-13 修订"记录段（口径统一 + 换端口说明），文档版本保持 V1.0 不变 |

**复核证据**（脚本全文扫描段落+全部 18 张表格）：
- `.bat` 残留：0 处（PASS，此前 635074e 的清理保持完整）。
- 版本号：无 2.1.0/2.2.0 等 API 版本字样错写；文档版本 V1.0 前后一致；无 V0.x 残留。
- 4173 剩余 3 处均为合理位置：§5.2 preview 可选验证说明、修订记录、§2.1 表（4173 行本身，说明列已改为"仅可选验证构建产物"）。
- 5173 现为唯一演示/验收入口口径，与 start-a23-dev.ps1 实际行为（`npm run dev -- --port 5173`）逐字对齐。

**仍开放**：终审材料清单所列"把'已验证可运行'记录补进文档"（L-vfy-08 结论入 docx）属 T6 终审定稿动作，本包未代办；文档尚未升版（由主控决定是否在 T6 升 V1.1）。

## 三、L-t5-04：dataIdentity.js 旧口径处置

**做了什么**（最小改动方案：保留文件路径，清空误导口径）：
- 全仓 grep 确认消费面：`dataIdentity` 仅被 `src/pages/Home.vue:100` 导入，且全文件唯一被使用的字段是 `identity.lakeName`（Home.vue:31）。
- 删除全部旧"全情景数据"时代字段：`dataMode=SCENARIO`、`dataModeLabel`、`DEMO-OBS-V1`、`DEMO-PRED-V1`、`DEMO-RUN-V1`、`asOfLabel/asOfFull`、`claimBoundary/claimBoundaryCode`、`claimNote`、`provenance[]`——这些与当前真实数据版本（TAIHU_CLEAN_FINAL_V1_20260831，observed/simulated 双轨）不符且无任何消费方。
- 文件重写为：历史清理说明 + `@deprecated` 头注释 + `export const lakeName = '太湖'` + 兼容旧导入路径的 `export const dataIdentity = Object.freeze({ lakeName })`。Home.vue 未改动、零断裂。
- 头注释明确：数据身份口径以后端接口 meta 与页面实际数据身份标签为准，前端不得再硬编码全站口径文案（防未来误引用复发）。

**证据**：
- 修改后 grep `dataMode|DEMO-OBS-V1|DEMO-PRED-V1|DEMO-RUN-V1|simulation_only` 在 src/ 下仅命中 dataIdentity.js 自身的注释行（历史说明），无任何代码引用残留。
- `npm.cmd test`：**12/12 通过**（pass 12 / fail 0 / skipped 0，duration 2875ms），覆盖预测快照竞态、缓存世代、三态诊断不放宽等全部既有用例。

**仍开放**：无（该条可关闭）。

## 四、L-t5-05：无提示试用记录表

**做了什么**：新建 `tests/试用评审_无提示试用记录表_20260913.md`，包含：
- **"无提示"口径**（5 条硬规则：只发目标卡、主持人不指路、标准开场白、逐环计时、5 分钟超时判否）。
- **主持指南**：环境探活、表头三格登记方法（试用者编号 T-01~03 / 开始时间 / 页面口径版本照抄页面字样并注明用途=对齐数据与模型版本）、标准开场白全文、过程记录规则（误解/求援/原话三类，中性追问）、收尾流程。
- **8 张任务卡**：按产品主流程（发现关注位置→最新观测与历史→时效研判→驱动与可信→创建预警→匹配预案→记录处置→复盘），每张只写业务目标，不含任何页面名/路由/按钮/路径。
- **3 份空白记录表**（T-01/T-02/T-03）：表头含试用者编号、开始时间、页面口径版本、主持人；每环 5 列：任务/完成(是/否)/耗时秒/误解与求援记录/备注；末尾一句话总评。
- **汇总口径**：环节完成率 ≥2/3 判主流程可走通；单环中位 >300 秒标记可用性热点；同环误解 ≥2 次判系统性误导；三表口径版本不一致须拆分统计。
- **结果回填节**：试用执行后填写（当前为待执行）。

**证据/边界**：文档为可执行材料（含空白表与话术），本轮**不代替真实试用**——真实试用需 3 名试用者与主持人在系统运行窗口执行，结果回填后 L-t5-05 才可彻底关闭。

**仍开放**：按表组织 3 人真实试用并回填第六节。

## 五、L-vfy-07 scoped：干净副本 deploy_check 复测

**做了什么**：`git worktree add D:/Project/fuwai/_r3_lvf07_worktree freeze-r3-20260913`（detached HEAD 1323c78，1500 文件，主工作区零改动），在其中以主 venv 解释器运行 `scripts/deploy_check_a23.py --json`（只读探测，不杀进程不启服务），随后 `git worktree remove --force` 清理完毕（`git worktree list` 仅剩主工作区）。

**deploy_check_a23.py 干净副本结果数字**：
- **退出码 1（存在 FAIL，环境不可直接一键启动）；PASS=61 / WARN=2 / FAIL=6 / INFO=9**

| 分组 | 结果 |
| --- | --- |
| [1] Python 环境 | PASS 40（requirements.txt 在、Python 3.13.9 满足 ≥3.11、38 个钉版依赖经主 venv 全部可导入且版本一致）、INFO 2。backend/.venv 本身缺失（见 FAIL） |
| [2] Node/npm | PASS 2（node v24.15.0、npm 11.12.1）；**FAIL 4**：node_modules/{vite,vue,echarts,leaflet} 缺失 |
| [3] 关键路径 | PASS 14（后端入口/manifest/gate_table/package.json/vite.config/两个 ps1 含 UTF-8 BOM/数据包 manifest+三件套+simulated/**34 个 joblib 真身**）；**FAIL 2**：observations.parquet 缺失、backend/.venv 缺失；WARN 1：运行时实时目录四件套不全 |
| [4] 端口探测 | PASS 3（8000=A23 后端身份确认 2.2.0、5173=vite 代理链可达 A23、4173 空闲）；WARN 1：8001 被其他项目占用（本机环境态，非仓库问题） |
| [5] 幂等提示 | INFO 正确给出复用 8000、-BackendPort 8001 覆盖、前端无幂等会叠加实例等提示（与本次 docx 修订口径一致） |

**6 项 FAIL 全部定性**（4 项为干净副本预期自装项，2 项为同一结构性缺口）：
1. `backend/.venv` 缺失 —— 需按文档 §3.2 `python -m venv backend/.venv && pip install -r backend/requirements.txt`（本轮按红线未装全套依赖，38 钉版依赖已在主 venv 侧验证可导入，命令本身无障碍）。
2~5. `node_modules/{vite,vue,echarts,leaflet}` 缺失 —— 需按文档 §3.3 `npm ci`（package-lock.json 在，命令无障碍；未执行）。
6. **小型数据包 `observations.parquet` 缺失（结构性缺口，本包核心新发现）**：主工作区该文件存在（101,816 字节），但被 `.gitignore:73` 的全局规则 `*.parquet` 排除在 git 之外；`A23_小型运行数据包_V1.0.zip` 同样未被 git 跟踪。即：**纯 git clone 路径得到的副本四件套永远缺 observations.parquet，离线观测链不完整**（snapshots/stations/status 三件在 git）；四件套目前只随 zip/人工拷贝分发。模型 34 个 joblib 经抽检（718,082 字节/个）为真实文件、随 git 正常分发，不受该规则影响。

**按文档从零启动检查表**（对照修订后 docx 逐步）：

| 文档步骤 | 干净副本现状 | 判定 |
| --- | --- | --- |
| §3.1 获取源码（clone，含 Git LFS 提示） | 1500 文件齐全；模型 joblib 直接入库（本仓库实际未用 LFS），`git lfs pull` 对本仓库无实际作用（无害但多余） | 可行；文档 LFS 表述与仓库实际不符（小口径问题） |
| §3.2 建后端 venv + pip install | requirements.txt 完整、38 钉版依赖经主 venv 侧核对可导入；venv 本身需自建 | 可行（命令验证、未执行安装） |
| §3.3 npm ci + npm run build | package-lock.json 在；node_modules 需自装；dist/ 虽随 git 有旧产物，按文档应重新 build | 可行（未执行安装） |
| §4.2 放置离线快照四件套 | 三件在 git；**observations.parquet 不在 git 也不在 git 内的 zip** | **不可行（git 路径结构性缺口）**；随包 zip 分发路径可行 |
| §5.1 一键启动 start-a23-dev.ps1 | 脚本+BOM 在；前置 §3.2/§3.3 未完成则无法启动 | 前置完成后可行 |
| §6 部署验证（/api/health 等） | 依赖 §5.1 | 未走到（依赖链） |

**仍开放**：
- （建议入台账，新条目）`.gitignore` 全局 `*.parquet` 把小型运行数据包的关键观测文件与 zip 排除在 git 外——二选一处置：交付口径明确"数据包以 zip 人工拷贝为准，不入 git"并写入部署文档；或加白名单规则（`!企业提交材料/A23_小型运行数据包_V1.0/**`）让 git 路径自足。本轮未改 .gitignore（主控统一提交）。
- 文档 §3.1 的 Git LFS 表述与仓库实际（无 LFS 对象）不符，建议 T6 顺手改为"模型文件已随仓库直存"。
- 真·干净环境（无主 venv 兜底）的全量安装+启动+验收仍需在 R1 重训收尾、后端可重启窗口执行（原始 L-vfy-07 的完整闭环）。

## 六、npm test 结果

`npm.cmd test`（node --test tests/*.mjs）在 dataIdentity.js 重写后运行：**12/12 通过，0 fail，0 skipped**（pass 12 / duration 2875.5ms）。与 T5 轮 12/12 基线一致，无回归。

## 七、建议入台账条目（供主控）

| 编号 | 类型 | 内容 | 状态建议 |
| --- | --- | --- | --- |
| L-vfy-09 | 定案 | docx 七处修订：4173/5173 口径与 start-a23-dev.ps1 统一（演示=dev 5173，preview 4173 降为可选验证），补 -BackendPort 8001 换端口说明；复核无 .bat 残留、无版本号错写；原文件备份 _backups/…_20260913.docx | 待验证 → PASS |
| L-t5-04 | 定案 | dataIdentity.js 旧"全情景数据"口径整段删除，仅留 lakeName（唯一消费点 Home.vue 未动）；grep 无残留引用；npm test 12/12 | 待验证 → PASS |
| L-t5-05 | 部分关闭 | 试用记录表与任务卡已备（tests/试用评审_无提示试用记录表_20260913.md）；3 人真实试用执行+回填后彻底关闭 | 待验证 → 修复待复测（差真实试用） |
| L-vfy-07（scoped） | 部分关闭 | 干净 worktree（freeze-r3-20260913）deploy_check：PASS=61/WARN=2/FAIL=6，6 项 FAIL=venv+4 node_modules（自装项）+observations.parquet（结构性，见新条目）；按文档从零启动检查表完成；完整闭环待无重训窗口真装依赖 | 待验证 → 续期（scoped 部分完成） |
| （新）L-r3-02 建议 | 新发现 | .gitignore 全局 `*.parquet` 使 git 路径副本缺小型数据包 observations.parquet 与 zip，四件套仅在人工分发物中完整；需定"zip 为准"或加白名单 | 待处置 |

## 八、仍开放项汇总

1. L-t5-05 的 3 人真实无提示试用执行与结果回填（材料已备齐）。
2. L-vfy-07 完整闭环：无重训窗口时做真·干净环境全量安装（venv + npm ci + build）+ 启动 + §6 验收留痕。
3. `.gitignore` parquet 规则处置决策（zip 为准 or 白名单），及部署文档对应口径补充。
4. docx"已验证可运行"记录（L-vfy-08 结论）与版本号是否升 V1.1，留待 T6 终审定稿统一处理。
