# UI 设计交接文档

> **项目**：A23 · 蓝藻水华监测预警 · 数字孪生驾驶舱
> **类型**：服务外包大赛参赛项目（应用类 · 数据与计算赛道）
> **交接目的**：将现有前端 UI 交给专业设计师进行**全新视觉设计**。本文档说明项目背景、信息架构、数据内容与技术约束，供设计师在此基础上自由发挥。

---

## 一、项目是什么

面向**太湖**等富营养化重点湖库，构建「机理模型 × AI」融合的蓝藻水华**监测预警**系统。核心价值链路是：**早识别 → 早预报 → 早预警**。

- 用物理机理方程（藻类生长、水动力输运）保证可解释性
- 用 AI 模型（时序神经网络、集成学习）做预测与风险分级
- 两者融合，输出可解释、可校正的预测结果
- 以「数字孪生驾驶舱」的形式可视化展示

**一句话定位**：一套面向答辩演示的、科技感与专业感并重的数据可视化大屏 + 叙事介绍页面。

---

## 二、设计目标

> 现有 UI 为开发阶段的临时视觉（深色玻璃 + 青色），**可以完全推倒重来**。设计师拥有充分的视觉自由度，只需满足以下硬性约束：

1. **保留现有信息架构**（8 个页面及每页的核心内容块），布局可重排、组件可重设计。
2. **保留现有数据字段**（监测点位、预测档位、事件流等数据必须能展示）。
3. **保留三主题能力**（暗色 / 浅色 / 日式暖阳），或在此基础上重新设计主题体系。
4. **保留交互逻辑**（点位点击联动、时间档位切换、事件回放联动）。

除此之外，配色、字体、间距、圆角、图标、动效、布局 —— 全部可改。

---

## 三、信息架构与页面清单

共 **8 个页面**，分三类。

### 3.1 叙事 / 介绍类（4 页）

这类页面以「讲清楚项目」为目的，文字内容为主，适合用大标题 + 排版节奏。

| 路由 | 页面 | 定位 | 核心内容块 |
| --- | --- | --- | --- |
| `/` | **主页** | 门面，引导进入 | 品牌 Logo、系统状态（在线点位 + 实时时间）、主标题、副标语、核心数据（6 点位 / 未来 1~30 天 / 1 处红色预警）、主按钮（进入驾驶舱 / 项目概览）、湖体示意图 + 点位、三大模块入口列表、页脚 |
| `/project-overview` | **项目概览** | 01/03，讲问题与背景 | 4 个 meta 卡（主题/赛题/主办/核心）、背景段落、传统路径 vs 数据驱动两栏对比、融合范式方案、关键词流水（多源接入/质控/建模/孪生）、2 张现场图位 |
| `/tech-route` | **技术路线** | 02/03，讲怎么建 | 4 个 meta 卡、**5 阶段递进**（多源数据接入 → 机理建模 → AI 增强 → 可解释性 → 数字孪生平台），每阶段含：阶段号、标题、说明、要点列表、产物标签 |

### 3.2 驾驶舱总览（1 页）

| 路由 | 页面 | 定位 | 核心内容块 |
| --- | --- | --- | --- |
| `/cockpit` | **驾驶舱总览** | 03/03，三个子页的索引 | 4 个 meta 卡、说明段落、**3 张入口卡片**（监测站 / 风险热力 / 历史回放，各含编号、标题、描述、要点、进入按钮） |

### 3.3 驾驶舱子页（3 页）

这是**核心数据可视化页面**，是本次设计的重头戏。三页共享同一套顶部组件：**时间轴播放器** + **子页切换标签**，并共享同一份跨页状态（档位、选中点位、事件）。

| 路由 | 页面 | 核心内容块 |
| --- | --- | --- |
| `/stations` | **监测站档位研判** | 顶部标题、时间轴播放器、子页标签、4 个 KPI（监测点位/高风险/关注档位/选中点位）、**卫星地图（点位标记 + 图例）**、点位详情卡（风险徽章、4 项水质指标、预测文本、藻密度时序曲线、AI 分析 Tab、事件流、实景图位） |
| `/heatmap` | **风险热力分区** | 顶部标题、时间轴播放器、子页标签、4 个 KPI（高/中/低风险网格 + 置信度）、**19×11 风险热力图 + 图例**、风险摘要卡（热点分布前五、各点强度排行、机理+AI 置信曲线、湖泊底图位） |
| `/history` | **历史事件回放** | 顶部标题、时间轴播放器、子页标签、4 个 KPI（累计事件/高危/关注/当前档位）、**事件流列表**（15 条，可筛选日期）、事件详情卡（事件时间/档位/点位、推送按钮、事件说明、点位预测、时序曲线、因果链、现场影像位） |

---

## 四、关键数据结构（设计师需要知道有哪些数据）

### 4.1 监测点位（6 个）

分布在太湖，每个点位含：

| 字段 | 说明 |
| --- | --- |
| `id` / `short` / `name` | 标识 / 编号（如 NW-01）/ 名称（如「西北热点区」） |
| `risk` / `riskClass` | 风险标签（红色预警/橙色关注/绿色稳定）+ 等级（high/mid/low） |
| `summary` | 一句话概况 |
| `coord` | 经纬度（地图定位用） |
| `metrics` | 4 项水质指标：藻细胞密度、叶绿素 a、总磷、水温 |
| `forecast` | 5 档预测：每档有窗口、标题、正文 |
| `trend` | 24 点藻密度时序（画折线图） |
| `factors` | 因子贡献（如水温 88%、风速 76%…） |
| `timeline` | 点位事件流（时间、标题、说明） |
| `explainability` | SHAP 可解释因子（驱动因子 + 贡献度 + 方向） |

6 个点位：**西北热点区（红）、湖心浮标（橙）、入湖河口（橙）、东南监测站（绿）、取水口（绿）、南部通道（橙）**。

### 4.2 预测时间档位（5 档）

`未来 1 天 / 未来 3 天 / 未来 7 天 / 未来 15 天 / 未来 30 天`，对应短/中/长期尺度。这是全局联动的核心维度，切换档位时三个子页同步刷新。

### 4.3 事件流（15 条）

字段：时间、所属档位、关联点位、标题、摘要、严重程度（high/mid/low）。

### 4.4 风险热力网格

19×11 网格，每个格子 0–100 风险值，按档位变化。分级：≥75 高风险（红）、45–75 关注（橙）、≤45 稳定（绿）。

### 4.5 AI 分析（接入后端后）

- **模型对比**：机理模型 / AI 模型 1 / AI 模型 2 / 融合模型 的预测值条形对比 + 评估指标（R²、RMSE、MAE）+ 逐日预测结果
- **SHAP 解释**：正/负向因子统计、因子贡献排行条、敏感度曲线

---

## 五、技术约束（哪些能改、哪些不能动）

### 5.1 技术栈

- **Vue 3**（组合式 API）+ Vue Router 4（hash 模式）
- **Vite 7** 构建
- **ECharts 5**（折线 / 柱状 / 热力图）
- **Leaflet 1.9**（卫星地图，Esri World Imagery 瓦片）
- **原生 CSS 变量**做设计 token（无 Tailwind、无 UI 框架）

### 5.2 设计 token 体系（可重构）

现状是**两套 token 并存**（历史遗留），设计师可以直接接手重构为统一体系：

1. `tokens.css` — `--home-*` 系列，用于主页（Midnight 深色）
2. `src/styles.css` — `--teal` / `--coral` 等系列，用于其余页面

两者都支持三主题切换，通过 `<html data-theme="dark|light|sunrise">` 驱动。

### 5.3 地图（重要约束）

地图底图是**真实卫星影像**（Leaflet + Esri 瓦片），不可替换为自绘图形。可自定义的部分：

- 点位标记（当前是圆点 + 脉冲动画 + 标签）
- 图例、控件（缩放按钮、attribution）
- 图层切换按钮（卫星影像 / 地形地图）

### 5.4 图表

ECharts 图表数据已定，设计师需要提供**图表配色与样式规范**（轴线、文字、tooltip、图例的颜色），当前是硬编码的暗色系。

### 5.5 响应式

需适配桌面（大屏演示为主）与移动端（答辩现场可能用平板）。当前断点：1120 / 1080 / 1024 / 980 / 880 / 820 / 768 / 640 / 560 / 480。

---

## 六、现有视觉风格（仅供参考，可抛弃）

- **基调**：深色玻璃拟态（深蓝黑底 + 半透明面板 + 毛玻璃模糊）
- **主色**：湖青色 teal `#22d3c5`（暗色）/ `#0e9d95`（浅色）/ 柿红 `#d9643b`（暖阳）
- **辅助色**：珊瑚红（预警）、琥珀（关注）、薄荷绿（稳定）、紫罗兰（AI）
- **字体**：Bahnschrift（数字/英文标题）+ 微软雅黑 / 苹方（中文正文）
- **风格关键词**：科技感、数据大屏、政府报告气质、数字孪生

三主题预览（现有）：深色 Midnight → 浅色 Daylight → 日式暖阳 Sunrise。

---

## 七、已知待办 / 占位（设计师需知晓）

1. **6 处 `image-slot` 图片占位符**——需要真实图片或由设计重新定义这些位置（湖区现场照、团队合影、演示现场、驾驶舱全景、点位实景、历史回放封面、湖泊底图）。
2. **无全局导航栏**——目前页面靠内容里的链接跳转，无顶部导航。全新设计时可考虑加入统一导航。
3. **无 404 页面**。
4. **无 Logo 设计**——目前只有「A23 / LAKE TWIN」文字标，可重新设计品牌视觉。

---

## 八、期望的交付物

设计师完成后，请提供以下内容（按优先级）：

1. **设计稿**（Figma / Sketch 等，含 8 个页面的桌面 + 移动端关键屏）
2. **设计 token**（颜色、字体、字号、间距、圆角、阴影 —— 建议整理成可落地的 CSS 变量表）
3. **组件规范**（按钮、卡片、KPI、徽章、表格、图例、时间轴播放器、地图标记等复用组件的状态说明）
4. **图表配色规范**（ECharts 的轴线/文字/tooltip/系列色）
5. **图标**（如有需要）
6. **切图与标注**（地图标记、图标、背景纹理等）

---

## 九、协作说明

- 数据与交互逻辑已由前端确定，设计时**不要改动数据字段含义**，但可建议新增展示维度。
- 现有主题系统如需扩展（如新增主题），请同步给出该主题的完整 token 表。
- 设计定稿后，前端将按 token 与组件规范进行还原。
