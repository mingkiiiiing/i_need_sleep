# Data Factory audit source notes

Scope: `mvp_meiliangwan_2024 / baseline / seed 20260904 / SIM-V1`.

Primary inspected assets:

- `data-cleaning/data_factory/**`
- `data-cleaning/config/data_factory/generation_config.yaml`
- `data-cleaning/storage/runs/data_factory/mvp_meiliangwan_2024/**`
- `data-cleaning/storage/releases/data_factory_release/SIM-V1/**`
- `data-cleaning/tests/test_data_factory_*.py`

Important code evidence:

- `calibration/fitter.py:67-70` applies the split cutoff to weather.
- `calibration/fitter.py:100-208` passes water temperature, water quality, hydrology and observation-pattern tables to fitters without the same cutoff filter.
- `calibration/fitter.py:173-176` fits AR(1) to water-level differences and stores the returned difference mean as `level_mean_m`.
- `simulation/hydrology.py:69-76` selects that global fitted `level_mean_m`; `simulation/hydrology.py:95-97` clips level to 2–5 m and only reports post-clip bound hits.
- `simulation/remote_sensing.py:76-99` creates retrievals from latent simulated Chl-a and marks them synthetic.
- `labeling/tasks.py:163-182` converts those rows to satellite labels while hard-coding `is_syn=False` and `observed_*` status.
- `assembly/horizons.py:26-55` constructs features from latent simulation tables; the observation tables are not used for feature assembly.
- `assembly/horizons.py:145-150` prefers simulation truth when multiple target sources exist.
- `quality/acceptance.py` A07 checks only post-clip range; A15 checks issue date before target date, not feature-level source timestamps.
- `quality/veto.py` V10 only checks `is_synthetic AND is_ground_truth`, so identity laundering to `is_synthetic=false` passes.

Independent commands/results:

- Targeted tests: `35 passed in 4.94s`.
- Independent release SHA-256 verification: 0 mismatches.
- Independent audit script: `audit_data_generation.py`.
- Executed companion notebook: `数据生成独立质量审计.ipynb`.

Chart map:

- Section: risk overview.
- Question: how many findings exist at each severity?
- Family/type: comparison / bar.
- Fields: severity, findings.
- Takeaway: critical and high issues dominate; the output cannot enter formal experiments.
- Palette: single-root, restrained blue with neutral axes; labels carry the category distinction.
- Source: `audit_findings.csv` generated by the independent audit script.

Omissions:

- No trend chart was used because this is one completed generation run, not a history of comparable runs.
- No causal or predictive accuracy claim was made because there is no independent real-world test label set.
- The generator was not rerun because this was a read-only audit; existing outputs, manifests, code and hashes were inspected.
